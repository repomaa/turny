# Turny Frontend

SvelteKit SPA frontend for Turny — a Spotify RFID controller for Raspberry Pi.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The production build outputs to `build/` and is embedded into the Rust binary via `rust-embed`.
